# 🖼️ Avatar Image Setup

将您喜欢的头像图片保存到此目录，命名为 `avatar.png`。

## 推荐规格
- **尺寸**: 256x256 或 512x512 像素 (正方形)
- **格式**: PNG (支持透明背景)
- **内容**: 二次元角色头像

## 使用方法
1. 将图片复制到 `client/public/images/` 目录
2. 重命名为 `avatar.png`
3. 刷新页面即可看到新 Logo

如果没有放置图片，Logo 区域会显示一个渐变爱心图标作为后备。
